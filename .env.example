# ==========================================
#  AI EPG BRIDGE v2.0 - CONFIGURATION
# ==========================================

# --- GOOGLE GEMINI (AI ENGINE) ---
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here
# Recommended models: gemini-2.0-flash (fast), gemini-1.5-pro (better quality)
GEMINI_MODEL=gemini-2.0-flash

# --- XTREAM CODES (YOUR IPTV PROVIDER) ---
# The base URL given by your provider (e.g. http://provider.com:80)
# Do NOT include "/player_api.php" or trailing slashes
XC_URL=http://your.provider.com
XC_USERNAME=your_username
XC_PASSWORD=your_password

# --- OUTPUT SETTINGS ---
# Where the new EPG XML file will be saved (relative to project root)
OUTPUT_PATH=./data/epg_output.xml

# --- ADVANCED TUNING ---
# Minimum fuzzy match score (0-100) to accept a match
# Lower = more matches but potential mismatches
# Higher = fewer matches but more accurate
FUZZY_THRESHOLD=85

# Cache age in hours before re-downloading EPG sources
CACHE_MAX_AGE=24
